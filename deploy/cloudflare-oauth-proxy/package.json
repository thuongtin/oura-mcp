{
  "name": "oura-mcp-oauth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
